// coverage:ignore-file
// GENERATED BY Macro: DO NOT MODIFY BY HAND
// ignore_for_file: type=lint
// ignore_for_file: unused_element, unused_local_variable, unnecessary_overrides, deprecated_member_use, deprecated_member_use_from_same_package, use_function_type_syntax_for_parameters, unnecessary_const, avoid_init_to_null, invalid_override_different_default_values_named, prefer_expression_function_bodies, annotate_overrides, invalid_annotation_target, unnecessary_question_mark
// dart format off

part of '../../model/todo_model.dart';

/// A mixin for [TodoModel]. Don't forget to call [onInitState] in the constructor.
mixin TodoModelModel {
  @protected
  @pragma('vm:prefer-inline')
  @pragma('dart2js:tryInline')
  TodoModel get $ => this as TodoModel;

  @protected
  EffectFnInfo? $effectFnInfo;
  Set<void Function()> $untrackedFns = const {};

  // -------------------- States --------------------------
  final todosState = ValueNotifier<List<Todo>>([]);

  // -------------------- Computed ------------------------
  late final ValueNotifier<List<Todo>> completedTodosState = () {
    final state = ValueNotifier<List<Todo>>($._completedTodos);
    todosState.addListener($completedTodosDepsChanged);
    return state;
  }();

  void $completedTodosDepsChanged() {
    completedTodosState.value = $._completedTodos;
  }

  late final ValueNotifier<List<Todo>> incompleteTodosState = () {
    final state = ValueNotifier<List<Todo>>($._incompleteTodos);
    todosState.addListener($incompleteTodosDepsChanged);
    return state;
  }();

  void $incompleteTodosDepsChanged() {
    incompleteTodosState.value = $._incompleteTodos;
  }

  bool $initStateCalled = false;

  @mustCallSuper
  void onInitState() {
    if ($initStateCalled) return;
    $initStateCalled = true;

    final $ = this.$;

    todosState.value = $._todos;

    completedTodosState;
    incompleteTodosState;
  }

  void createEffect(ValueNotifier<Object?> notifier, void Function() fn) {
    $effectFnInfo ??= {};
    MacroEffectUtils.createEffect(notifier, fn, $effectFnInfo!, $untrackedFns);
  }

  void addEffect(ValueNotifier<Object?> dependency, void Function() fn) {
    $effectFnInfo ??= {};
    MacroEffectUtils.removeEffect(dependency, fn, $effectFnInfo);
    MacroEffectUtils.createEffect(dependency, fn, $effectFnInfo!, $untrackedFns);
  }

  void removeEffect(ValueNotifier<Object?> dependency, void Function() fn) {
    MacroEffectUtils.removeEffect(dependency, fn, $effectFnInfo);
  }

  void untracked(
    void Function() fn, {
    void Function()? effectFn,
    List<void Function()>? effectFns,
  }) async {
    if ($untrackedFns == const <void Function()>{}) {
      $untrackedFns = {};
    }
    MacroEffectUtils.untracked(
      fn,
      effectFn: effectFn,
      effectFns: effectFns,
      effectFnInfo: $effectFnInfo,
      untrackedFns: $untrackedFns,
    );
  }

  void dispose() {
    todosState.dispose();
    completedTodosState.dispose();
    incompleteTodosState.dispose();
  }
}
