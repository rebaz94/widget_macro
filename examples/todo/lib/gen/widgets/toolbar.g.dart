// coverage:ignore-file
// GENERATED BY Macro: DO NOT MODIFY BY HAND
// ignore_for_file: type=lint
// ignore_for_file: unused_element, unused_local_variable, unnecessary_overrides, deprecated_member_use, deprecated_member_use_from_same_package, use_function_type_syntax_for_parameters, unnecessary_const, avoid_init_to_null, invalid_override_different_default_values_named, prefer_expression_function_bodies, annotate_overrides, invalid_annotation_target, unnecessary_question_mark
// dart format off

part of '../../widgets/toolbar.dart';

abstract class _BaseToolbarState extends State<Toolbar> with BaseStateMixin<Toolbar> {
  @protected
  @pragma('vm:prefer-inline')
  @pragma('dart2js:tryInline')
  _ToolbarState get $ => this as _ToolbarState;

  // -------------------- Environments --------------------
  late final TodoModel todosModel = Provider.of(context, listen: false);
  late final ValueNotifier<TodosFilter> currentFilter = Provider.of(context, listen: false);

  // -------------------- Computed ------------------------
  late final ValueNotifier<int> allTodosCountState = () {
    final state = ValueNotifier<int>($._allTodosCount);
    todosModel.todosState.addListener($allTodosCountDepsChanged);
    return state;
  }();

  void $allTodosCountDepsChanged() {
    allTodosCountState.value = $._allTodosCount;
  }

  late final ValueNotifier<int> incompleteTodosCountState = () {
    final state = ValueNotifier<int>($._incompleteTodosCount);
    todosModel.incompleteTodosState.addListener($incompleteTodosCountDepsChanged);
    return state;
  }();

  void $incompleteTodosCountDepsChanged() {
    incompleteTodosCountState.value = $._incompleteTodosCount;
  }

  late final ValueNotifier<int> completedTodosCountState = () {
    final state = ValueNotifier<int>($._completedTodosCount);
    todosModel.completedTodosState.addListener($completedTodosCountDepsChanged);
    return state;
  }();

  void $completedTodosCountDepsChanged() {
    completedTodosCountState.value = $._completedTodosCount;
  }

  @mustCallSuper
  void onInitState() {
    final $ = this.$;
    currentFilter;

    allTodosCountState;
    incompleteTodosCountState;
    completedTodosCountState;

    createEffect(todosModel.completedTodosState, $.onCompletedToDoChanged);
  }

  @override
  @mustCallSuper
  void didChangeDependencies() {
    if (!initStateCalled) {
      onInitState();
      initStateCalled = true;
      super.didChangeDependencies();
      return;
    }

    super.didChangeDependencies();
  }

  @override
  void didUpdateWidget(covariant Toolbar old) {
    super.didUpdateWidget(old);
  }

  @override
  void dispose() {
    removeEffect(todosModel.completedTodosState, $.onCompletedToDoChanged);
    todosModel.todosState.removeListener($allTodosCountDepsChanged);
    allTodosCountState.dispose();
    todosModel.incompleteTodosState.removeListener($incompleteTodosCountDepsChanged);
    incompleteTodosCountState.dispose();
    todosModel.completedTodosState.removeListener($completedTodosCountDepsChanged);
    completedTodosCountState.dispose();
    super.dispose();
  }
}
